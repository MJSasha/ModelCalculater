@using UI.Components.Dialogs.Base
@inherits BaseDialog<InputDialogParams, string>

<BaseDialogLayout IsShown="IsShown">
    <BaseModal Title="@Parameters.Title" OnClose="() => this.Cancel()" DispalyCenter="true">
        <Content>
            <input type="text" class="form-control mt-2" autofocus @bind="Value" />
        </Content>
        <Footer>
            <div class="mt-3">
                <ModalButton class="btn-outline-danger" OnClick="() => this.Cancel()">@LocalizationService.Localization.Common_Cancel</ModalButton>
                <ModalButton class="btn-outline-success" OnClick="() => this.Close(Value)">@LocalizationService.Localization.Common_Ok</ModalButton>
            </div>
        </Footer>
    </BaseModal>
</BaseDialogLayout>

@code {
    protected string Value { get; set; }

    protected override void OnShow()
    {
        Value = "";
        StateHasChanged();
    }
}